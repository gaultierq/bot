# Migration `20200913152020-add-created-at`

This migration has been generated by Quentin Gaultier at 9/13/2020, 6:20:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Bot" ADD COLUMN "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP;

ALTER TABLE "public"."Interaction" ADD COLUMN "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP;

ALTER TABLE "public"."Post" ADD COLUMN "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP;

ALTER TABLE "public"."User" ADD COLUMN "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200913063349-add-interaction..20200913152020-add-created-at
--- datamodel.dml
+++ datamodel.dml
@@ -4,20 +4,22 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
-  id    String @default(cuid()) @id
-  email String @unique
-  name  String
-  posts Post[]
+  id        String @default(cuid()) @id
+  createdAt DateTime    @default(now())
+  email     String @unique
+  name      String
+  posts     Post[]
 }
 model Post {
   id        String  @default(cuid()) @id
+  createdAt DateTime    @default(now())
   authorId  String?
   content   String?
   published Boolean @default(false)
   title     String
@@ -25,8 +27,9 @@
 }
 model Bot {
   id        String  @default(cuid()) @id
+  createdAt DateTime    @default(now())
   authorId  String?
   published Boolean @default(false)
   title     String
   image     String?
@@ -34,8 +37,9 @@
 }
 model Interaction {
   id        String  @default(cuid()) @id
+  createdAt DateTime    @default(now())
   botId     String?
   content   String
   bot       Bot?   @relation(fields: [botId], references: [id])
 }
```


